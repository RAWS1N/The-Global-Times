import React from 'react'
import { Header, NavLinks, SearchBox } from '../components'

function HeaderContainer() {
  return (
    <div className="">
        <Header />
        <NavLinks/>
        <SearchBox/>
    </div>
  )
}

export default HeaderContainer